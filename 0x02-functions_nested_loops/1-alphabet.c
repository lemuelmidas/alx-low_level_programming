#include "main.h"

/**
 * print_alphabet: print alphabet in 
 *        lower case followed by a new line
 * Return : nothing
 */
void print_alphabet(void)
{
        char k;
        for (k= 'a'; k <= 'z'; k++);
                _putchar(k);
        _putchar('\n');
  
}
